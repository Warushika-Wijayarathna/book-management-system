export interface Reader {
  _id: string;
  name: string;
  email: string;
  contactNumber: string;
  address: string;
  createdAt: string;
}

export interface ReaderFormData {
  name: string;
  email: string;
  contactNumber: string;
  address: string;
  createdAt: string;
}

